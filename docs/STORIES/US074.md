# US074 — `syntek-manifest`: Module Manifest Spec & CLI Shared Framework

**Last Updated**: 08/03/2026 **Version**: 0.1.0 **Status**: Completed **Epic**: Epic 1 — Foundation
& Tooling

---

## User Story

As a module author, I want a standardised manifest format and a shared Rust CLI library so that
every Syntek module can ship its own self-contained installer binary without independently inventing
the scaffolding protocol.

## MoSCoW Priority

**Must Have**

## Acceptance Criteria

```gherkin
Given a valid `syntek.manifest.toml` file
When the CLI framework parses it
Then all declared fields (module ID, options, settings keys, entry points) are validated and any missing required fields raise a descriptive error

Given a developer runs a module's installer binary with no arguments
When the binary starts
Then it presents an interactive prompt for each install option declared in the manifest

Given a backend module installer runs and the developer selects options
When the installer completes
Then the correct `SYNTEK_<MODULE>` config block is written to `settings.py` and the app is added to `INSTALLED_APPS` with no duplicate entries

Given a frontend module installer runs and the developer selects options
When the installer completes
Then the project's app root is wrapped with required providers and a minimal usage stub is written to the declared entry file

Given any installer completes
When post-install steps exist (URL patterns, migration commands, native build steps)
Then they are printed as formatted copy-paste snippets — never written to files automatically

Given a `settings.py` already contains a `SYNTEK_<MODULE>` block
When the installer runs again
Then it detects the existing block, warns the developer, and does not overwrite without explicit confirmation
```

## Dependencies

- US001 — Monorepo Workspace Configuration

## Tasks

- [x] **Red phase:** write failing tests — manifest parsing, option prompts, settings.py writer,
      provider wrapper, duplicate detection, post-install output format
- [x] Define `syntek.manifest.toml` schema — fields: `id`, `version`, `kind` (rust-crate | backend |
      frontend | mobile), `options[]`, `settings[]`, `installed_apps`, `providers[]`, `entry_point`,
      `post_install_steps[]`
- [x] Create `rust/syntek-manifest/` Cargo crate — the shared library all module CLI binaries link
      against
- [x] Implement manifest parser (TOML → validated struct with descriptive errors on
      missing/wrong-type fields)
- [x] Implement interactive option prompter — renders each `options[]` entry as a checkbox/select
      prompt
- [x] Implement `settings.py` writer — reads `settings[]` from manifest, writes typed config block,
      respects existing blocks with confirmation prompt
- [x] Implement `INSTALLED_APPS` writer — appends app entry without duplicating
- [x] Implement provider wrapper — reads `providers[]`, wraps the declared `entry_point` file
- [x] Implement post-install printer — renders `post_install_steps[]` as formatted copy-paste output
      with syntax highlighting
- [x] Implement duplicate detection — scans target files for existing `SYNTEK_<MODULE>` blocks
      before writing
- [x] Publish `syntek-manifest` to the Forgejo Cargo registry so all module CLI crates can depend on
      it
- [x] Write `syntek.manifest.toml` authoring guide (schema reference, worked example for a backend
      module and a frontend package)
- [x] **Green phase:** all parser, writer, prompter, and printer tests pass

## Story Points

**5**

---

## Completion Summary

**Completed**: 08/03/2026\
**Branch**: `us074/module-manifest-spec-cli-shared-framework`\
**Implementation**: `rust/syntek-manifest/`

### Test Results

- **127 / 127 tests passing** (`cargo test -p syntek-manifest`)
- All six integration test suites green: `parser_tests`, `prompter_tests`, `settings_writer_tests`,
  `provider_wrapper_tests`, `post_install_tests`, `duplicate_detector_tests`
- `cargo clippy -p syntek-manifest -- -D warnings` — clean
- `cargo fmt --check -p syntek-manifest` — clean

### QA Findings Resolved

| Severity         | Count | Status   |
| ---------------- | ----- | -------- |
| Critical         | 2     | Resolved |
| High             | 6     | Resolved |
| Medium           | 6     | Resolved |
| Low              | 6     | Resolved |
| MC coverage gaps | 7     | Resolved |

### Acceptance Criteria Status

All six Gherkin scenarios from the story are satisfied:

- Valid `syntek.manifest.toml` parses with all fields validated and descriptive errors on missing
  required fields.
- Interactive prompt is rendered for each install option declared in the manifest.
- Correct `SYNTEK_<MODULE>` config block is written to `settings.py`; app is added to
  `INSTALLED_APPS` with no duplicate entries.
- Frontend app root is wrapped with required providers; minimal usage stub is written to the
  declared entry file.
- Post-install steps are printed as formatted copy-paste snippets — never written to files.
- Existing `SYNTEK_<MODULE>` blocks are detected and the developer is warned before any overwrite.

### Notes

- `syntek-manifest` is published to the Forgejo Cargo registry and is ready for all module CLI
  binaries (US006 onwards) to declare as a dependency.
- The `syntek.manifest.toml` authoring guide has been written with a worked example for both a
  backend module and a frontend package.
