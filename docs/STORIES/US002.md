# US002 — Shared TypeScript Types Package

**Last Updated**: 06/03/2026
**Version**: 0.1.0
**Status**: To Do
**Epic**: Epic 1 — Foundation & Workspace

---

## User Story

As a frontend developer, I want a shared TypeScript types package so that all web and mobile packages use consistent type definitions without duplication.

## MoSCoW Priority

**Must Have**

## Acceptance Criteria

```gherkin
Given the shared/types package is installed in a workspace package
When I import a type
Then TypeScript resolves it without errors

Given a type is defined in shared/types
When I change its shape in a breaking way
Then TypeScript compilation fails in all consuming packages that are incompatible

Given shared/types is built
When I check the dist output
Then declaration files (.d.ts) are present for all exports
```

## Dependencies

- US001 — Monorepo Workspace Configuration

## Tasks

- [ ] **Red phase:** write failing tests — type export verification, cross-package import resolution, .d.ts output
- [ ] Create `shared/types/` with `tsconfig.json`, `package.json` (`@syntek/types`)
- [ ] Define base entity types: `ID`, `Timestamp`, `PaginatedResponse<T>`, `ApiError`
- [ ] Define auth types: `User`, `Session`, `Permission`, `Role`
- [ ] Define tenant types: `Tenant`, `TenantSettings`
- [ ] Define notification types: `Notification`, `NotificationChannel`
- [ ] Export all types from `index.ts`
- [ ] Add to pnpm workspace
- [ ] **Green phase:** all type tests pass

## Story Points

**3**
