# US004 — Shared GraphQL Operations Package

**Last Updated**: 06/03/2026
**Version**: 0.1.0
**Status**: To Do
**Epic**: Epic 1 — Foundation & Workspace

---

## User Story

As a frontend developer, I want pre-generated, typed GraphQL operations so that all packages query the backend consistently with full TypeScript inference.

## MoSCoW Priority

**Must Have**

## Acceptance Criteria

```gherkin
Given the shared/graphql package is built
When I import a query hook in any web package
Then TypeScript infers the correct response and variable types

Given a query is defined in a .graphql file
When I run `pnpm codegen`
Then typed React Query hooks are generated for that operation

Given the backend GraphQL schema changes
When I run `pnpm codegen`
Then the generated types update and TypeScript surfaces any breaking changes in consuming packages

Given codegen runs in CI
When the output differs from the committed files
Then the CI pipeline fails, requiring the developer to commit updated generated files
```

## Dependencies

- US001 — Monorepo Workspace Configuration
- US002 — Shared TypeScript Types Package

## Tasks

- [ ] **Red phase:** write failing tests — codegen output is valid TypeScript, all operations type-check, schema drift detection
- [ ] Create `shared/graphql/` package (`@syntek/graphql`)
- [ ] Configure `graphql-codegen` with schema URL/SDL and output paths
- [ ] Define initial `.graphql` operation files: auth queries/mutations, tenant query
- [ ] Generate typed React Query hooks via `@graphql-codegen/typescript-react-query`
- [ ] Add `pnpm codegen` script to root `package.json`
- [ ] Add schema drift check to CI pipeline
- [ ] **Green phase:** codegen runs clean, all generated types type-check

## Story Points

**3**
