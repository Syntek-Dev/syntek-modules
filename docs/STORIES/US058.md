# US058 — `@syntek/mobile-auth`: Mobile Authentication

**Last Updated**: 06/03/2026 **Version**: 0.1.0 **Status**: To Do **Epic**: Epic 7 — Mobile Packages

---

## User Story

As a developer, I want a mobile authentication package so that I can add biometric login, social
OAuth, deep link handling, and passkey support to React Native applications backed by `syntek-auth`.

## MoSCoW Priority

**Must Have**

## Acceptance Criteria

```gherkin
Given biometrics are enrolled on the device
When the user opens the app with a valid session token stored in the secure keychain
Then Face ID / Touch ID / fingerprint is used to unlock the session without re-entering credentials

Given a social OAuth button is tapped
When the OAuth flow completes in the in-app browser
Then the deep link returns the auth code, the backend mutation exchanges it, and the session is established

Given the app is opened via a magic link deep link
When the deep link is intercepted
Then the token is extracted, validated with the backend, and the user is logged in

Given the user registers a passkey
When the WebAuthn ceremony completes on device
Then the passkey credential is stored in the device's platform authenticator and usable for future logins
```

## Dependencies

- US057 — `@syntek/mobile-ui`
- US009 — `syntek-auth` (GraphQL mutations)
- US044 — `@syntek/api-client`

## Tasks

- [ ] **Red phase:** write failing tests — biometric unlock, OAuth deep link exchange, magic link
      extraction, passkey ceremony
- [ ] Set up `mobile/mobile-auth/` package
- [ ] Implement `BiometricLogin` using Expo LocalAuthentication
- [ ] Implement secure token storage using Expo SecureStore (not AsyncStorage)
- [ ] Implement social OAuth with in-app browser (Expo AuthSession)
- [ ] Implement deep link handler for magic links and OAuth callbacks
- [ ] Implement passkey registration and assertion (WebAuthn via react-native-passkeys or Expo
      equivalent)
- [ ] Implement `LoginScreen`, `RegisterScreen`, `MFAScreen` native UI
- [ ] Create `syntek.manifest.toml` — package ID, available install options, required providers,
      native module linking requirements, and Expo config plugin declarations
- [ ] Build Rust CLI installer binary — reads manifest, presents install options interactively,
      configures providers in the app entry, adds Expo config plugin entries, and prints native
      setup steps (e.g. `npx expo prebuild`)
- [ ] **Green phase:** all mobile auth tests pass on iOS and Android simulators

## Story Points

**8**
