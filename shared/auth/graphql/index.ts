/**
 * GraphQL operations index.
 *
 * Central export for all authentication-related GraphQL operations,
 * including client configuration, fragments, queries, and mutations.
 *
 * @example
 * ```typescript
 * import { authApolloClient, GET_CURRENT_USER, LOGIN } from '@syntek/shared-auth/graphql';
 * ```
 */

export * from './client';
export * from './fragments';
export * from './queries';
export * from './mutations';
